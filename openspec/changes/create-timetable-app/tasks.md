## 1. プロジェクト初期設定・基盤構築

- [x] 1.1 Vite + React + TypeScript + Tailwind CSS プロジェクトのセットアップを行い、ビルドが成功することを確認する
- [x] 1.2 `lucide-react`, `clsx`, `tailwind-merge` 等のUI依存パッケージを導入し、開発サーバーが正常に起動することを確認する
- [x] 1.3 時間割、講義、課題、出欠、設定に関するTypeScript型定義（`types/index.ts`）を作成し、型エラーがないことを確認する

## 2. ストレージ＆状態管理レイヤーの実装

- [x] 2.1 LocalStorage永続化マネージャーおよび初期サンプルデータ（2026年度前期）の生成ロジックを実装し、初回起動時にサンプルが表示され、リロード後もデータが保持されることを確認する
- [x] 2.2 時間割（Timetable）および講義（Lecture）のCRUD操作を行う状態管理ロジック（Context / Hooks）を実装し、講義の追加・編集・削除がリアクティブに反映されることを確認する
- [x] 2.3 課題（Assignment）および出欠記録（AttendanceRecord）の状態管理フックを実装し、ステータス更新や出欠カウントの加減算が正常に動作することを確認する

## 3. UIコンポーネントと時間割グリッドの実装

- [x] 3.1 アプリケーションの全体レイアウト（ヘッダー、学期切り替えメニュー、ナビゲーション、ダーク/ライトテーマ切り替え）を実装し、デザインと表示が正常であることを確認する
- [x] 3.2 曜日×時限の時間割グリッド（TimetableGrid）および講義セル（LectureCard）を実装し、設定された曜日・時限・テーマカラーに沿ってグリッドが表示されることを確認する
- [x] 3.3 講義の登録・編集・削除モーダル（LectureModal）を実装し、空きコマクリックからの新規作成や既存講義の編集・削除が正常に行えることを確認する
- [x] 3.4 スマートフォン・小型画面向けに「今日の講義一覧」および「曜日別切り替えビュー」を実装し、レスポンシブに表示が最適化されることを確認する

## 4. 課題管理・出欠管理機能の実装

- [x] 4.1 課題一覧・ダッシュボード（AssignmentDashboard）を実装し、期日順ソート、未完了/完了フィルタ、期日直前のアラートバッジ表示が動作することを確認する
- [x] 4.2 講義ごとの出欠管理（出席/欠席/遅刻/休講）および単位リスク警告（規定欠席数超過時のバッジ通知）を実装し、講義カードおよびモーダル内で正しくカウント・警告されることを確認する

## 5. エクスポート・共有・設定機能の実装

- [ ] 5.1 JSON形式での全データエクスポート（バックアップ）およびJSONファイルのインポート（データ復元・バリデーション）機能を実装し、データ移行が成功することを確認する
- [ ] 5.2 時間割データをiCalendar形式（.ics）でエクスポートする機能を実装し、主要カレンダーアプリに取り込めるフォーマットでダウンロードできることを確認する
- [ ] 5.3 時間割グリッドの画像（PNG）ダウンロードおよび印刷用スタイルシート（Print View）を実装し、画像出力および印刷プレビューが正常に機能することを確認する
- [ ] 5.4 設定モーダル（時限数変更 5〜7限、土日表示/非表示、各時限の時間帯設定、新規時間割の追加・削除）を実装し、設定変更がグリッドに即座に反映されることを確認する

## 6. 総合検証と仕上げ

- [ ] 6.1 すべての機能（講義CRUD、課題管理、出欠カウント、データインポート/エクスポート、レスポンシブ表示、テーマ切り替え）を通しで検証し、プロダクションビルド（`npm run build`）が正常に通ることを確認する
